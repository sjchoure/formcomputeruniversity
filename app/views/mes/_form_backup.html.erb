
<%= form_with(model: me, local: true) do |form| %>
  <% if me.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(me.errors.count, "error") %> prohibited this me from being saved:</h2>

      <ul>
        <% me.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control', autofocus: true, placeholder: 'Full name',:required => 'required'  %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, class: 'form-control', placeholder: 'email@example.com',:required => 'required' %>
  </div>

  <div class="actions">
    <%= form.submit 'Sign Up',class: 'btn btn-success'%>
  </div>
<% end %>

